
# Compilation of the project
steps:
  - name: 'docker/compose:1.19.0'
    args: ['up', '-d'] 

  - name: 'gcr.io/cloud-builders/gradle'
    args: ['buildProduct', '-i']

  - name: 'ubuntu' 
    args: ['bash', './front/build/output/front/start.sh']
